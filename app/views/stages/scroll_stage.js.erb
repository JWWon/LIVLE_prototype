$('#list-container').append('<%= j render @stages %>');
<% if @stages.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @stages %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>